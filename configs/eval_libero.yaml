# LIBERO closed-loop evaluation config.
task_suite: libero_spatial   # libero_spatial | libero_object | libero_goal | libero_10
num_episodes: 50
seeds: [0, 1, 2]
max_steps: 520               # overridden per-suite in libero_runner.py when null
action_horizon: 10
replan_steps: 5
resize_size: 224
num_steps_wait: 10
render: true
video_out: results/rollouts
host: 0.0.0.0
port: 8000
